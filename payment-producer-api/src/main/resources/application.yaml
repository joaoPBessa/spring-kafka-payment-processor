spring:
  application:
    name: payment-producer-api
  jackson:
    property-naming-strategy: SNAKE_CASE

  datasource:
    url: jdbc:postgresql://localhost:5432/payments_db
    username: payment-producer-app
    password: K1SJdZqbkJyW^qM)
    driver-class-name: org.postgresql.Driver
    hikari:
      schema: payment
      maximum-pool-size: 10
  jpa:
   show-sql: true
   properties:
    hibernate:
     format_sql: true
     highlight_sql: true
  data:
   redis:
    host: localhost
    port: 6379
    timeout: 2000ms
    connect-timeout: 5000ms
    lettuce:
     pool:
      max-active: 8
      max-idle: 8
      min-idle: 0
      max-wait: -1ms

  flyway:
    enabled: true
    url: jdbc:postgresql://localhost:5432/payments_db
    user: ${FLYWAY_DB_USER:payment-admin}
    password: ${FLYWAY_DB_PASS:qU0+ketjd]65]q7D}
    schemas: payment
    locations: classpath:db/migration
