# --- JAVA & MAVEN ---
target/
*.class
*.jar
*.war
*.ear
*.nar
*.stackdump
hs_err_pid*
HELP.md

# --- ECLIPSE ---
.settings/
.project
.classpath
.metadata/
bin/
*.launch
.factorypath
.springBeans

# --- IDEs ADICIONAIS (Caso algum colega use) ---
.idea/
*.iml
.vscode/

# --- SISTEMA OPERACIONAL ---
.DS_Store
Thumbs.db

# --- CONFIGURAÇÕES LOCAIS ---
# Descomente se você tiver senhas ou chaves em arquivos locais
# application-local.yaml
# .env

# --- ARTEFATOS DE IA (locais, não fazem parte do código do projeto) ---
CLAUDE.md
.claude/

# --- AVRO GENERATED SOURCES ---
# Only .avsc schema files are committed. Generated Java classes are build
# output — already covered by target/ above, listed explicitly here so the
# intent is documented — and must be regenerated locally (or in CI) via
# `mvn generate-sources`, never committed.
**/generated-sources/
